import React from 'react';
import { Outlet } from "react-router-dom"

const IndexReviews = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default IndexReviews;