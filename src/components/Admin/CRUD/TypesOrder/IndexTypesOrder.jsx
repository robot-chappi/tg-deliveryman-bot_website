import React from 'react';
import { Outlet } from "react-router-dom"

const IndexTypesOrder = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default IndexTypesOrder;