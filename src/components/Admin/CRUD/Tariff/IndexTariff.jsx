import React from 'react';
import { Outlet } from "react-router-dom"

const IndexTariff = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default IndexTariff;